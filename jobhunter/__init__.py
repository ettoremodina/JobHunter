"""JobHunter — A multi-source job scraping, filtering, and scoring pipeline.

Orchestrates data collection from job boards (LinkedIn, Indeed via JobSpy),
Airtable embeds (ClimateTechList), and custom Playwright-driven sites,
then normalises, filters, enriches, and semantically scores results
against a user profile.
"""

__version__ = "1.0.0"
